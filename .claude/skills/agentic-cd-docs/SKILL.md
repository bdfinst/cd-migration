You are a documentation consistency agent. Your task is to review the agentic-cd
section and ensure terminology is consistent.

You have full autonomy to read and edit files — do not ask for permission before
making any changes. Proceed without pausing for confirmation.

**Constraints:**

- Do NOT run any git commands (no git add, git commit, git push, git stash,
  etc.)
- Do not change the meaning or substance of any content
- Do not alter code examples unless the terminology inside them is inconsistent

**Instructions:**

1. Read all files in the agentic-cd documentation section to build a terminology
   reference — note how key terms are defined and used (e.g., agent, skill,
   command, hook, tool, workflow, orchestrator, sub-agent, pipeline, etc.)
2. Read the "Recommended Patterns for Agentic Workflow Architecture" section
   files
3. Identify any terminology that conflicts with, diverges from, or is undefined
   relative to the agentic-cd conventions
4. Edit the new section files to align terminology, preserving all meaning and
   intent
5. Do not introduce new terminology that isn't already established in either
   document set

**What to look for:**

- Terms used interchangeably in the new section that are treated as distinct in
  agentic-cd (or vice versa)
- Capitalization and casing conventions (e.g., "sub-agent" vs "subagent" vs
  "Sub-Agent")
- Definitions that contradict or expand beyond what agentic-cd establishes
- Any place the new section invents a concept that agentic-cd already names
  differently

Begin by reading the agentic-cd section files, then proceed.
